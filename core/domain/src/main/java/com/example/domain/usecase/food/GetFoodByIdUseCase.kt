package com.example.domain.usecase.food

import com.example.domain.model.food.Food
import com.example.domain.repository.food.FoodRepository
import javax.inject.Inject

//class GetFoodByIdUseCase @Inject constructor(
//    private val repository: FoodRepository
//) {
//    suspend operator fun invoke(id: String): Food? {
//        return repository.getFoodById(id)
//    }
//}